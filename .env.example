# Copy this to .env and fill in real values
# NEVER commit .env to git

# Database
DB_PASSWORD=change_me_to_a_strong_random_password

# Optional: override API URL for local dev
# PUBLIC_API_URL=http://localhost:8080
